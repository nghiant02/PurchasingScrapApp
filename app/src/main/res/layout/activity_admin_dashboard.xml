<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <Button
        android:id="@+id/button_manage_users"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Manage Users"
        android:layout_marginBottom="16dp" />

    <Button
        android:id="@+id/button_manage_scrap_posts"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Manage Scrap Posts"
        android:layout_below="@id/button_manage_users"
        android:layout_marginBottom="16dp" />

    <Button
        android:id="@+id/button_assign_tasks"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Assign Tasks"
        android:layout_below="@id/button_manage_scrap_posts"
        android:layout_marginBottom="16dp" />

    <Button
        android:id="@+id/button_view_reports"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="View Reports"
        android:layout_below="@id/button_assign_tasks"
        android:layout_marginBottom="16dp" />
</RelativeLayout>
